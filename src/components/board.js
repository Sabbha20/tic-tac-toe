import React from "react";

const Board = () => {
    return <div style={{
        backgroundColor: "skyblue",
        margin: 10,
        padding:20
    }}>
    Board
    </div>
}

export default Board;